name: Build And Push MusicTV Unity Project to Steam Beta

on:
    push:
        branches:
            - main
        paths:
            - "MusicTV/SongQuiz/**"
            - ".github/workflows/build-push-musictv-beta.yml"
    workflow_dispatch:

permissions:
    contents: read

env:
    STEAM_APP_ID: "1315390"

jobs:
    buildForSteam:
        name: Build for ${{ matrix.targetPlatform }}
        runs-on: ubuntu-latest
        strategy:
            fail-fast: false
            matrix:
                targetPlatform:
                    - StandaloneWindows64
                    - StandaloneLinux64
                    - StandaloneOSX

        steps:
            - name: Checkout repository
              uses: actions/checkout@v4
              with:
                  fetch-depth: 0
                  lfs: true

            - name: Cache Unity Library
              uses: actions/cache@v4
              with:
                  path: MusicTV/SongQuiz/Library
                  key: Library-${{ matrix.targetPlatform }}-${{ hashFiles('MusicTV/SongQuiz/Assets/**', 'MusicTV/SongQuiz/Packages/**', 'MusicTV/SongQuiz/ProjectSettings/**') }}
                  restore-keys: |
                      Library-${{ matrix.targetPlatform }}-
                      Library-

            - name: Compute Unity build version
              id: unityBuildVersion
              run: |
                  PROJECT_VERSION=$(grep -m1 '^  bundleVersion:' MusicTV/SongQuiz/ProjectSettings/ProjectSettings.asset | sed 's/^  bundleVersion:[[:space:]]*//')
                  if [ -z "$PROJECT_VERSION" ]; then
                      echo "Could not read bundleVersion from ProjectSettings.asset" >&2
                      exit 1
                  fi
                  echo "version=$PROJECT_VERSION.${{ github.run_number }}" >> "$GITHUB_OUTPUT"

            - name: Build Unity project
              uses: game-ci/unity-builder@v4
              env:
                  UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
                  UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
                  UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
              with:
                  projectPath: MusicTV/SongQuiz
                  targetPlatform: ${{ matrix.targetPlatform }}
                  unityVersion: auto
                  versioning: Custom
                  version: ${{ steps.unityBuildVersion.outputs.version }}

            - name: Upload build artifact
              uses: actions/upload-artifact@v4
              with:
                  name: Build-${{ matrix.targetPlatform }}
                  path: build/${{ matrix.targetPlatform }}

    deployToSteam:
        name: Deploy to Steam Beta
        needs: buildForSteam
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Download StandaloneWindows64 artifact
              uses: actions/download-artifact@v4
              with:
                  name: Build-StandaloneWindows64
                  path: build/verse.surf

            - name: Download StandaloneLinux64 artifact
              uses: actions/download-artifact@v4
              with:
                  name: Build-StandaloneLinux64
                  path: build/verse.surf.x64

            - name: Download StandaloneOSX artifact
              uses: actions/download-artifact@v4
              with:
                  name: Build-StandaloneOSX
                  path: build/verse.surf.app

            - name: Deploy build to Steam Beta
              uses: game-ci/steam-deploy@v3
              with:
                  username: ${{ secrets.STEAM_USERNAME }}
                  configVdf: ${{ secrets.STEAM_CONFIG_VDF }}
                  appId: ${{ env.STEAM_APP_ID }}
                  buildDescription: Beta build ${{ github.run_number }} (${{ github.sha }})
                  rootPath: build
                  depot1Path: verse.surf
                  depot9Path: verse.surf.x64
                  depot8Path: verse.surf.app
                  releaseBranch: beta
